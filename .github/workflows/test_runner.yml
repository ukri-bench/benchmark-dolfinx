name: Lint test

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]
  merge_group:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint-doc:
    name: ROCm SMI
    runs-on: self-hosted
    container:
      image: ghcr.io/ukri-bench/spack-buildcache-rocm:16081115783f33f8ed1fc75a4d093fd97665c150
      options: --device /dev/kfd --device /dev/dri --security-opt seccomp=unconfined
    steps:
      - name: Install GPU compiler/driver
        uses: ukri-bench/spack-configs/actions/install-gpu-driver@main
        with:
          runtime: rocm

      - name: ruff Python checks
        run: |
          pwd
          echo $USER
          rocm-smi
          bench_dolfinx --ndofs=10000 --degree=3 --qmode=0 --nreps=1 --mat_comp --float=64 --json a.json

  # lint-cpp:
  #   name: Code lint checks
  #   runs-on: self-hosted
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #     - name: Install linting tools
  #       run: pip install clang-format ruff
  #     - name: ruff Python checks
  #       run: |
  #         ruff check .
  #         ruff format --check .
  #     - name: clang-format C++ checks
  #       run: |
  #         cd src
  #         clang-format --version
  #         find . -type f \( -name "*.cpp" -o -name "*.h" -o -name "*.hpp" \) | xargs clang-format --dry-run --Werror
